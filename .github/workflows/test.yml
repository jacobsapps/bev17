name: Test
on:
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: macOS
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v3

      - name: Run Unit Tests
        run: fastlane test